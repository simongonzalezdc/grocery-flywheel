# Expected Fixture Outputs

The CLI writes deterministic state, analysis, and dashboard artifacts from
`examples/imports/example-history.json`. Tests assert the contract directly so
large generated JSON snapshots do not become brittle review noise.
